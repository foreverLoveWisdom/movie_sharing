- flash.each do |name, msg|
  - if msg.is_a?(String)
    .alert{ class: "alert-#{name == 'alert' ? 'danger' : 'success'} alert-dismissible fade show", role: "alert" }
      %button{ type: "button", class: "btn-close", "data-bs-dismiss": "alert", "aria-label": "Close" }
      = msg
